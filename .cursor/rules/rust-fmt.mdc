---
description: Run rustfmt at the end of Rust work in this repo
alwaysApply: true
---

# Rust formatting

Before finishing any task that touches Rust code in this workspace, run:

```bash
cargo fmt --all
```

If formatting changes files, include them in the same commit or follow-up fix commit.
